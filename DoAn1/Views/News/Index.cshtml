@model IEnumerable<DoAn1.Models.News>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title_news)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image_news)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Content_news)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title_news)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image_news)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Content_news)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.NewsID }) |
                @Html.ActionLink("Details", "Details", new { id=item.NewsID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.NewsID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
